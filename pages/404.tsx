import { NextPage } from "next";
import Head from "next/head";

import { useEffect } from "react";

const NotFound: NextPage = () => {
  return (
    <>
      {/* 
          Idea for the 404 version that will go online. 
          A page where it just shows the little cave where 8bit link gets the sword where the old man says "It's too dangerous to go forth, 
          take this" and link does his little animation where the item that pops up is a button back to the homepage." 
      */}
    </>
  );
};

export default NotFound;
